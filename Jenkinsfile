pipeline {
    agent {
        docker {
            image 'registry.bst-1.cns.bstjpc.com:5000/library/ubuntu:latest'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'ls /'
            }
        }
    }
}
