apiVersion: v1
kind: Service
metadata:
  name: {{ template "chart-test.fullname" . }}
  labels:
    app: {{ template "chart-test.name" . }}
    chart: {{ template "chart-test.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "chart-test.name" . }}
    release: {{ .Release.Name }}
