apiVersion: v1
kind: Service
metadata:
  name: {{ template "registry-ui.fullname" . }}
  labels:
    app: {{ template "registry-ui.name" . }}
    chart: {{ template "registry-ui.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "registry-ui.name" . }}
    release: {{ .Release.Name }}
