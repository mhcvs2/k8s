apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fio-pv2-claim3
  labels:
    app: fio-test3
spec:
  storageClassName: rook-block
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi